
f = open("./inputs/day18.txt", "r")
lines = readlines(f)

for (i, line) in enumerate(lines)
    line = split(line,' ')
    l = line[1]
    dir = line[2]
    ##for (j, sym) in enumerate(line)

    ##end
end